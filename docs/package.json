{
  "name": "drippia-icons-docs",
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "npm run build && npx wrangler pages deploy dist --project-name=drippia-icons --commit-dirty=true"
  },
  "dependencies": {
    "astro": "^4.16.19"
  }
}
